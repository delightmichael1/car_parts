import { useState } from "react"; import { MdArrowForward, MdCheck } from "react-icons/md"; export default function RecordForm({type}:{type:"Product"|"Customer"}){const [saved,setSaved]=useState(false); return <div className="mx-auto flex max-w-2xl flex-col gap-6 rounded-3xl border border-border bg-card p-6 md:p-8"><div><span className="font-mono text-[10px] uppercase tracking-[0.24em] text-primary">New record</span><h1 className="mt-2 text-3xl font-semibold text-secondary">Add {type.toLowerCase()}</h1><p className="mt-2 text-sm text-muted-foreground">Capture the details your team needs at the counter.</p></div><div className="grid gap-4 sm:grid-cols-2">{(type==="Product"?["Part name","SKU","Category","Unit price","Opening stock","Reorder level"]:["Full name","Phone number","Email address","Vehicle make","Vehicle model","Notes"]).map((label)=><label key={label} className="flex flex-col gap-2 text-sm font-medium text-secondary">{label}<input className="min-h-12 rounded-xl border border-border bg-background-2 px-4 outline-none focus:border-primary" placeholder={`Enter ${label.toLowerCase()}`} /></label>)}</div><button onClick={()=>setSaved(true)} className="flex min-h-14 items-center justify-center gap-2 rounded-2xl bg-secondary text-sm font-semibold text-background">{saved?<><MdCheck/>Saved</>:<>Save {type}<MdArrowForward/></>}</button></div>}
